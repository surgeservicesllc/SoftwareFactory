import { ServicesSchedulePanel } from "@/components/services/schedule-panel";

export const metadata = { title: "Schedule" };

export default function ServicesSchedulePage() {
  return <ServicesSchedulePanel />;
}
