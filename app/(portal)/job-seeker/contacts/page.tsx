import { JobSeekerContactsPanel } from "@/components/job-seeker/contacts-panel";

export const metadata = { title: "Contacts & Outreach" };

export default function ContactsPage() {
  return <JobSeekerContactsPanel />;
}
