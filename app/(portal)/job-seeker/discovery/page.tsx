import { JobSeekerConsole } from "@/components/job-seeker/console";

export const metadata = { title: "Job Search" };

export default function JobDiscoveryPage() {
  return <JobSeekerConsole section="discovery" />;
}
