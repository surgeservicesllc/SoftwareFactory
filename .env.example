# SoftwareFactory environment template
# Copy to .env.local and keep real values out of source control.
# Empty values mean the associated live integration is Not Connected.

# Browser-public application URL used for absolute metadata links.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Browser-public Supabase configuration (not privileged; RLS is mandatory)
NEXT_PUBLIC_SUPABASE_URL=
# Preferred client credential.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy fallback accepted by the client.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Trusted local development only. Keep false in hosted/production environments.
# When true, the allowlisted repository Markdown editor may write local files.
SOFTWAREFACTORY_ENABLE_LOCAL_FILE_WRITES=false

# Future server-only Supabase credential. Phase 1A request handlers do not use it;
# user JWTs, RLS, and reviewed SECURITY DEFINER RPCs enforce access.
# Never prefix this value with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# Local/CI migration tooling only; do not expose at application runtime.
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_ID=
SUPABASE_DB_PASSWORD=

# Future GitHub App integration — Not Connected in Phase 1A.
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=

# Future deployment and AI-provider integrations — Not Connected in Phase 1A.
VERCEL_TOKEN=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
