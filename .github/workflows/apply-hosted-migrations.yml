name: Apply hosted migrations

# The runbook (AI/HOSTED_APPLY_RUNBOOK.md), executable: repairs the one
# renamed ledger row, then pushes every outstanding migration to the hosted
# Supabase project — exactly the steps the owner would type, with the ledger
# listed before and after so the run's log is its own evidence.
#
# Owner-triggered only, and it refuses to run without the typed confirmation,
# because this writes DDL to production. Secrets (Settings → Secrets and
# variables → Actions):
#   SUPABASE_ACCESS_TOKEN  — a personal access token (looks like sbp_0102…)
#   SUPABASE_DB_PASSWORD   — the project database password
# A valid token selects the linked project flow; a missing or malformed token
# falls back to a direct database connection through the IPv4 pooler. Surgical
# scopes that verify live catalog identity with psql also require the database
# password even in token mode (the direct project hostname is IPv6-only, which
# GitHub-hosted runners cannot reach).
# Never paste either secret into a chat, an issue, or a commit.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "apply" to run against the production project'
        required: true
      scope:
        description: >-
          "all" pushes outstanding ordinary migrations only after protected
          20260822000200 and 20260822000300 are each recorded by their dedicated
          scopes; "broker-functions"
          applies only the idempotent broker migrations
          (20260816000400 through 20260820000300, plus 20260821000300) and
          records them in history;
          "project-controls" applies the 20260815 range plus the two run-review
          files, which is what the project detail page and the Runs page need;
          "pipeline-selection" applies only 20260821000300, which is what makes
          the AI Factory's Use button record a project's pipelines;
          "bot-account-binding" applies only 20260822000200, which binds a
          subscription bot to its exact tenant-scoped AI account;
          "bot-account-binding-contract" applies only 20260822000300, which
          revokes the six legacy bot-mutator execute grants after exact-app
          deployment and signed-in production acceptance;
          "lifecycle" applies only the two Agentic SDLC files
          (20260821000100 and 20260821000200) and records them in history;
          "probe" changes nothing at all and just reports which objects already
          exist, because the ledger has proven to understate the live schema.
        required: false
        default: probe
        type: choice
        options:
          - all
          - broker-functions
          - project-controls
          - pipeline-selection
          - agent-selection
          - bot-account-binding
          - bot-account-binding-contract
          - lifecycle
          - probe
      contract_app_sha:
        description: >-
          CONTRACT only: exact 40-character application SHA on main, exposed by
          the latest successful Vercel-bot Production deployment and accepted by an owner
        required: false
      contract_acceptance:
        description: >-
          CONTRACT only: type "exact-app-vercel-accepted" after verifying the
          named SHA is healthy on Vercel project prj_pAsrhftaVWI4SyaqstgRVSWHJkdD
        required: false

permissions:
  contents: read
  deployments: read

concurrency:
  group: apply-hosted-migrations
  cancel-in-progress: false

jobs:
  apply:
    name: Repair ledger and push migrations
    if: ${{ inputs.confirm == 'apply' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      PROJECT_REF: qpuofpmagrmyamahqwxw
    steps:
      - name: Check out the migrations
        # actions/checkout v4 (reviewed immutable commit)
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false

      - name: Verify exact application acceptance before CONTRACT preflight
        if: ${{ inputs.scope == 'bot-account-binding-contract' }}
        env:
          CONTRACT_APP_SHA: ${{ inputs.contract_app_sha }}
          CONTRACT_ACCEPTANCE: ${{ inputs.contract_acceptance }}
          GH_API_URL: ${{ github.api_url }}
          GH_REF: ${{ github.ref }}
          GH_REPOSITORY: ${{ github.repository }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CHECKED_OUT_SHA=$(git rev-parse HEAD)
          if [ "$GH_REF" != "refs/heads/main" ]; then
            echo "::error::CONTRACT may be dispatched only from refs/heads/main."
            exit 1
          fi
          if [[ ! "$CONTRACT_APP_SHA" =~ ^[a-f0-9]{40}$ ]] || [ "$CONTRACT_APP_SHA" != "$CHECKED_OUT_SHA" ]; then
            echo "::error::CONTRACT requires the exact checked-out 40-character application SHA already accepted in Vercel production."
            exit 1
          fi
          if [ "$CONTRACT_ACCEPTANCE" != "exact-app-vercel-accepted" ]; then
            echo "::error::CONTRACT requires the exact-app-vercel-accepted attestation after signed-in production acceptance."
            exit 1
          fi
          DEPLOYMENTS=$(curl --fail --silent --show-error \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "${GH_API_URL}/repos/${GH_REPOSITORY}/deployments?environment=Production&per_page=100")
          LATEST_VERCEL_DEPLOYMENT=$(jq -c '[
            .[] | select(
              .environment == "Production"
              and .task == "deploy"
              and .creator.login == "vercel[bot]"
            )
          ][0] // empty' <<<"$DEPLOYMENTS")
          if [ -z "$LATEST_VERCEL_DEPLOYMENT" ] || ! jq -e --arg sha "$CONTRACT_APP_SHA" '
            .sha == $sha and .ref == $sha
          ' <<<"$LATEST_VERCEL_DEPLOYMENT" >/dev/null; then
            echo "::error::The latest Vercel-bot Production deployment is not the exact checked-out main SHA/ref."
            exit 1
          fi

          STATUS_URL=$(jq -r '.statuses_url' <<<"$LATEST_VERCEL_DEPLOYMENT")
          LATEST_STATUS=$(curl --fail --silent --show-error \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "${STATUS_URL}?per_page=1")
          if ! jq -e '
            length == 1
            and .[0].state == "success"
            and .[0].creator.login == "vercel[bot]"
            and (.[0].environment_url | type == "string")
            and (.[0].environment_url | test("^https://[^/]+\\.vercel\\.app(/|$)"))
          ' <<<"$LATEST_STATUS" >/dev/null; then
            echo "::error::The exact-SHA Vercel Production deployment has no latest successful Vercel-bot status."
            exit 1
          fi
          VERCEL_DEPLOYMENT_URL=$(jq -r '.[0].environment_url' <<<"$LATEST_STATUS")
          echo "::notice::Exact main SHA ${CONTRACT_APP_SHA} has a successful Vercel Production deployment at ${VERCEL_DEPLOYMENT_URL}; the operator also attests signed-in owner acceptance."

      - name: Choose the connection
        # Decides MODE=token (valid sbp_ token, linked flow) or MODE=dburl
        # (password-only, through the IPv4 session pooler). The URL-encoded
        # password and the full URL are masked explicitly: only the raw
        # secret value is masked automatically, and this repository is
        # public.
        run: |
          if [[ "$SUPABASE_ACCESS_TOKEN" =~ ^sbp_[A-Za-z0-9]{16,}$ ]]; then
            echo "MODE=token" >> "$GITHUB_ENV"
            echo "Using the personal access token (linked project flow)."
          elif [ -n "$SUPABASE_DB_PASSWORD" ]; then
            echo "SUPABASE_ACCESS_TOKEN is missing or not shaped like a personal access token (they look like sbp_0102...1920; generate one at supabase.com -> Account -> Access Tokens)."
            echo "Falling back to the direct database connection using SUPABASE_DB_PASSWORD only."
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            FOUND=""
            for HOST in aws-0-us-west-2.pooler.supabase.com aws-1-us-west-2.pooler.supabase.com; do
              URL="postgresql://postgres.${PROJECT_REF}:${ENC}@${HOST}:5432/postgres"
              echo "::add-mask::$URL"
              if npx --yes supabase@2 migration list --db-url "$URL" >/dev/null 2>&1; then
                echo "Reached the database through ${HOST}."
                echo "MODE=dburl" >> "$GITHUB_ENV"
                echo "DB_URL=$URL" >> "$GITHUB_ENV"
                FOUND="yes"
                break
              fi
            done
            if [ -z "$FOUND" ]; then
              echo "::error::Could not reach the database through the pooler (tried aws-0/aws-1 us-west-2). Either SUPABASE_DB_PASSWORD is wrong, or the project lives elsewhere — set a valid SUPABASE_ACCESS_TOKEN (sbp_...) to use the linked flow instead."
              exit 1
            fi
          else
            echo "::error::Neither a valid SUPABASE_ACCESS_TOKEN (sbp_...) nor SUPABASE_DB_PASSWORD is set. Add at least the database password as an Actions secret."
            exit 1
          fi

      - name: Link the production project (token mode only)
        if: ${{ env.MODE == 'token' }}
        run: npx --yes supabase@2 link --project-ref "$PROJECT_REF"

      - name: List the remote ledger before touching anything
        run: |
          if [ "$MODE" = "token" ]; then
            npx supabase migration list --linked
          else
            npx supabase migration list --db-url "$DB_URL"
          fi

      - name: Repair stale ledger rows (idempotent)
        # Never on a probe. `scope: probe` is documented as changing nothing at
        # all, and it is the step an operator reaches for precisely when they
        # do not yet trust the ledger -- but this step sits above the scope
        # branch, so a probe could still rewrite history rows. A read-only
        # question must not be able to edit the thing being questioned.
        if: ${{ inputs.scope != 'probe' && inputs.scope != 'bot-account-binding' && inputs.scope != 'bot-account-binding-contract' }}
        # Two known history-only repairs, each applied only when the live
        # listing shows the stale row. 20260814002000 was renamed to
        # 20260814000100; remote-only 20260814000200 is the pre-split name
        # of 20260814000210/000220/000250, all three of which the remote
        # ledger already records — reverting the stale row is bookkeeping,
        # not DDL. If neither row is present, this is a no-op.
        run: |
          if [ "$MODE" = "token" ]; then
            LIST=$(npx supabase migration list --linked)
          else
            LIST=$(npx supabase migration list --db-url "$DB_URL")
          fi
          repair() {
            if [ "$MODE" = "token" ]; then
              npx supabase migration repair --status "$1" "$2"
            else
              npx supabase migration repair --status "$1" "$2" --db-url "$DB_URL"
            fi
          }
          if echo "$LIST" | grep -q "20260814002000"; then
            echo "Repairing: 20260814002000 -> 20260814000100 (history only, no DDL)"
            repair reverted 20260814002000
            repair applied 20260814000100
          else
            echo "No 20260814002000 row found; that repair is not needed."
          fi
          if echo "$LIST" | grep -q '` `.*20260814000200'; then
            echo "Reverting stale remote-only row 20260814000200 (its DDL lives on as 000210/000220/000250, all recorded)"
            repair reverted 20260814000200
          else
            echo "No remote-only 20260814000200 row found; that repair is not needed."
          fi

      - name: Apply the broker function migrations surgically (scope=broker-functions)
        # Every file here is idempotent by construction — `create or replace
        # function`, `create table if not exists`, `create index if not
        # exists`, `alter type ... add value if not exists`, and grants — so
        # applying them directly cannot disturb existing schema. psql runs
        # each statement autocommitted, which is what lets an enum label be
        # added and then referenced from a function body defined below it. psql is preinstalled on the runner, and
        # Supabase's DDL event trigger reloads the PostgREST schema cache
        # so the functions become callable immediately. History rows are
        # recorded so the ledger stays truthful.
        if: ${{ inputs.scope == 'broker-functions' }}
        run: |
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          for FILE in \
            supabase/migrations/20260816000400_inspect_ai_auth_sessions.sql \
            supabase/migrations/20260816000500_remove_ai_account.sql \
            supabase/migrations/20260816000600_ai_account_identity.sql \
            supabase/migrations/20260816000700_rename_bot.sql \
            supabase/migrations/20260816000800_cancel_discards_pending_account.sql \
            supabase/migrations/20260816000900_read_session_status.sql \
            supabase/migrations/20260816001000_reapply_vault_read.sql \
            supabase/migrations/20260816001100_vault_envelope_size.sql \
            supabase/migrations/20260816001200_device_flow_verifying.sql \
            supabase/migrations/20260816001300_replan_never_started_run.sql \
            supabase/migrations/20260816001400_project_repository_picker.sql \
            supabase/migrations/20260816001500_ai_account_usage_observations.sql \
            supabase/migrations/20260817000100_project_edit_details.sql \
            supabase/migrations/20260817000200_run_review_activity_types.sql \
            supabase/migrations/20260817000300_run_review_and_deletion.sql \
            supabase/migrations/20260817000400_task_work_lock_leases.sql \
            supabase/migrations/20260817000500_clear_all_finished_runs.sql \
            supabase/migrations/20260817000600_owner_operated_safety_controls.sql \
            supabase/migrations/20260817000700_bot_assignment_configuration.sql \
            supabase/migrations/20260817000800_custom_pipeline_templates.sql \
            supabase/migrations/20260817000900_run_and_report_lifecycle_types.sql \
            supabase/migrations/20260817001000_archive_runs_and_clear_reports.sql \
            supabase/migrations/20260817001100_assignment_model_and_effort.sql \
            supabase/migrations/20260818000100_removable_accounts_keep_usage_evidence.sql \
            supabase/migrations/20260818000200_seed_standard_model_catalogue.sql \
            supabase/migrations/20260819000100_graph_worker_execution.sql \
            supabase/migrations/20260819000200_replant_exhausted_graph.sql \
            supabase/migrations/20260819000300_tolerant_fan_in.sql \
            supabase/migrations/20260819000400_list_graph_runs.sql \
            supabase/migrations/20260819000500_replant_with_room_to_work.sql \
            supabase/migrations/20260819000600_tolerant_fan_ins_for_planted_copy.sql \
            supabase/migrations/20260819000700_bot_credential_ref_privileged_parity.sql \
            supabase/migrations/20260819000800_list_graph_run_verifications.sql \
            supabase/migrations/20260819000900_record_verification_as_worker.sql \
            supabase/migrations/20260819001000_claim_by_executor_capability.sql \
            supabase/migrations/20260819001100_usage_survives_a_rate_limited_probe.sql \
            supabase/migrations/20260819001200_replant_for_the_complete_run.sql \
            supabase/migrations/20260820000100_job_seeker_activity_types.sql \
            supabase/migrations/20260820000200_job_seeker_foundation.sql \
            supabase/migrations/20260820000300_job_seeker_resume_upload.sql \
            supabase/migrations/20260821000300_project_pipeline_selection.sql
          do
            echo "Applying ${FILE}"
            psql "$DB_URL" -v ON_ERROR_STOP=1 -q -f "$FILE"
          done
          repair() {
            if [ "$MODE" = "token" ]; then
              npx supabase migration repair --status applied "$1"
            else
              npx supabase migration repair --status applied "$1" --db-url "$DB_URL"
            fi
          }
          repair 20260816000400
          repair 20260816000500
          repair 20260816000600
          repair 20260816000700
          repair 20260816000800
          repair 20260816000900
          repair 20260816001000
          repair 20260816001100
          repair 20260816001200
          repair 20260816001300
          repair 20260816001400
          repair 20260816001500
          repair 20260817000100
          repair 20260817000200
          repair 20260817000300
          repair 20260817000400
          repair 20260817000500
          repair 20260817000600
          repair 20260817000700
          repair 20260817000800
          repair 20260817000900
          repair 20260817001000
          repair 20260817001100
          repair 20260818000100
          repair 20260818000200
          repair 20260819000100
          repair 20260819000200
          repair 20260819000300
          repair 20260819000400
          repair 20260819000500
          repair 20260819000600
          repair 20260819000700
          repair 20260819000800
          repair 20260819000900
          repair 20260819001000
          repair 20260819001100
          repair 20260819001200
          repair 20260820000100
          repair 20260820000200
          repair 20260820000300
          repair 20260821000300
          # Supabase's DDL event trigger reloads the PostgREST schema cache
          # on its own; this explicit reload is insurance, so a browser that
          # saw PGRST202 seconds ago cannot keep seeing it.
          psql "$DB_URL" -q -c "NOTIFY pgrst, 'reload schema';"
          echo "Broker function migrations applied, recorded, and the API schema cache reloaded."

      - name: Report what the database already has (scope=probe)
        # Read-only, and the only honest basis for an apply decision.
        #
        # Run 32068091179 proved the ledger understates production: it listed
        # the whole 20260815 range as unapplied, and applying it found the enum
        # labels and organizations.maximum_concurrent_runs already present —
        # live DDL with no history row. Run 32103778884 then proved the ledger
        # is not even a contiguous prefix: it is missing 19 versions in the
        # middle while carrying every row of the 20260817 range above them.
        #
        # The first query is the one that decides what to do. It names the
        # marker object each ledger-absent migration introduces and prints a
        # `present` boolean per version, so an absent object is visible as `f`
        # rather than as a row that simply never appears — the flaw in the
        # older query, which listed only what existed and so could not tell
        # "missing" apart from "not asked about".
        #
        # present = t  ->  the DDL is live; repair the history row, do not re-run the file.
        # present = f  ->  the file has genuinely not run; applying it is real DDL.
        #
        # Two of the nineteen only replace an existing function, so existence
        # proves nothing about them. Those are probed by a substring of the
        # function body that the migration introduced.
        if: ${{ inputs.scope == 'probe' }}
        run: |
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            with expected(version, kind, object, marker) as (values
              ('20260814002500', 'table',    'provider_credentials',                 null),
              ('20260814002600', 'function', 'store_provider_credential',            null),
              ('20260815000200', 'table',    'scheduling_decisions',                 null),
              ('20260815000300', 'function', 'portfolio_capacity_verdict',           null),
              ('20260815000400', 'body',     'plan_phase1c_task_and_run',            'agent.project_id = new.project_id'),
              ('20260815000500', 'function', 'breaker_cooldown_seconds',             null),
              ('20260815000600', 'function', 'portfolio_scheduling_queue',           null),
              ('20260815000800', 'body',     'generate_operations_report',           'phase1e-operations-v2'),
              ('20260815000900', 'function', 'refuse_project_deletion',              null),
              ('20260815001000', 'function', 'declare_cross_project_dependency',     null),
              ('20260815001100', 'table',    'connection_routing_decisions',         null),
              ('20260815001200', 'table',    'improvement_ledger',                   null),
              ('20260815001300', 'function', 'capture_improvement_baseline',         null),
              ('20260815001400', 'function', 'audit_factory_health',                 null),
              ('20260815001500', 'function', 'detect_factory_improvements',          null),
              ('20260815001600', 'function', 'propose_improvements_from_detections', null),
              ('20260816000100', 'table',    'ai_accounts',                          null),
              ('20260816000200', 'function', 'list_ai_accounts_for_verification',    null),
              ('20260816000300', 'function', 'find_open_ai_auth_session',            null)
            )
            select expected.version,
                   expected.kind,
                   expected.object,
                   case expected.kind
                     when 'table' then exists (
                       select 1 from pg_class relation
                         join pg_namespace space on space.oid = relation.relnamespace
                        where space.nspname = 'public'
                          and relation.relkind = 'r'
                          and relation.relname = expected.object)
                     when 'function' then exists (
                       select 1 from pg_proc routine
                         join pg_namespace space on space.oid = routine.pronamespace
                        where space.nspname = 'public'
                          and routine.proname = expected.object)
                     else exists (
                       select 1 from pg_proc routine
                         join pg_namespace space on space.oid = routine.pronamespace
                        where space.nspname = 'public'
                          and routine.proname = expected.object
                          and strpos(pg_get_functiondef(routine.oid), expected.marker) > 0)
                   end as present
              from expected
             order by expected.version;"
          echo "present = t: the DDL is live, so repair the history row rather than re-running the file."
          echo "present = f: that migration has genuinely not run."
          echo ""
          echo "Frozen pre-EXPAND register_bot identity (read-only catalog evidence)."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            set search_path = pg_catalog;
            select routine.oid::regprocedure::text as signature,
                   md5(pg_get_functiondef(routine.oid)) as definition_md5,
                   pg_get_userbyid(routine.proowner) as owner,
                   routine_language.lanname as language,
                   routine.prokind,
                   routine.provolatile,
                   routine.prosecdef as security_definer,
                   routine.proconfig,
                   routine.proacl
              from (values (
                'public.register_bot(uuid,text,public.bot_provider,text,text,text,text)'
              )) expected(signature)
              left join pg_proc routine on routine.oid = to_regprocedure(expected.signature)
              left join pg_language routine_language on routine_language.oid = routine.prolang;"
          echo "Expected definition_md5 before EXPAND: 318700d6d8d26603d9f5572f11d16e2f."
          echo ""
          echo "The ADR-036 credential denylist, checked directly rather than through the ledger."
          echo "Version 20260819000700 was recorded once while a DIFFERENT file's DDL ran under"
          echo "that number (see AI/HOSTED_APPLY_RUNBOOK.md), so its history row proves nothing."
          echo "This asks the constraint itself, and names every privileged reference it covers."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select conname,
                   (strpos(pg_get_constraintdef(oid), 'SUPABASE_SECRET_KEY') > 0
                    and strpos(pg_get_constraintdef(oid), 'SUPABASE_ACCESS_TOKEN') > 0
                    and strpos(pg_get_constraintdef(oid), 'VERCEL_OIDC_TOKEN') > 0
                    and strpos(pg_get_constraintdef(oid), 'POSTGRES_URL') > 0
                    and strpos(pg_get_constraintdef(oid), 'GITHUB_CLIENT_ID') > 0) as covers_all_five_added
              from pg_constraint
             where conname = 'bots_credential_ref_not_privileged';"
          echo "no row = the constraint is absent; covers_all_five_added = f means the parity fix has not run."
          echo ""
          echo "The last five usage observations, newest first — the measured evidence behind"
          echo "the Bot Manager's usage line. The detail column is constraint-guaranteed"
          echo "secret-free (<=400 chars, text_has_likely_secret refused), so printing it in a"
          echo "log is safe, and it is the only way to read WHY a probe failed without a"
          echo "database console: the counts in the auth-broker log say only that it failed."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select observed_at, status, left(coalesce(detail, ''), 200) as detail
              from public.ai_account_usage_observations
             order by observed_at desc
             limit 5;" || echo "usage observations table absent: migration 20260816001500 has not run here."
          echo ""
          echo "Ownership and grants for the AI-account section, which is where a 42501 comes from."
          echo "A SECURITY DEFINER function runs as its owner: if the function and the tables it"
          echo "writes were applied by different roles, the function can lack privileges on them"
          echo "and the console reports 'permission denied for table ...' as a bare 42501."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select 'function' as kind,
                   routine.proname as name,
                   pg_get_userbyid(routine.proowner) as owner,
                   routine.prosecdef as security_definer,
                   has_function_privilege('authenticated', routine.oid, 'EXECUTE') as authenticated_may_execute
              from pg_proc routine
              join pg_namespace space on space.oid = routine.pronamespace
             where space.nspname = 'public'
               and routine.proname in (
                 'remove_ai_account','disconnect_ai_account','rename_ai_account',
                 'create_ai_account','can_manage_organization','has_organization_role')
            union all
            select 'table',
                   relation.relname,
                   pg_get_userbyid(relation.relowner),
                   relation.relrowsecurity and relation.relforcerowsecurity,
                   null
              from pg_class relation
              join pg_namespace space on space.oid = relation.relnamespace
             where space.nspname = 'public'
               and relation.relkind = 'r'
               and relation.relname in (
                 'ai_accounts','ai_auth_sessions','provider_credentials',
                 'activity_events','bots')
            order by 1, 2;"
          echo "For a table row the boolean column is FORCE row level security, not execute."
          echo "Every function above should share one owner with every table above."
          echo ""
          echo "Does the owner bypass RLS, and can a definer function write an audit row at all?"
          echo "FORCE row level security subjects the table owner to its own policies, and only a"
          echo "superuser or a BYPASSRLS role escapes that. activity_events carries exactly one"
          echo "policy and it is SELECT, so an owner that does not bypass cannot insert an audit"
          echo "row - which is the last statement of remove_ai_account, and a 42501."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select rolname, rolsuper, rolbypassrls
              from pg_roles
             where rolname in ('postgres', 'authenticated', 'service_role')
             order by rolname;"
          cat > /tmp/audit-write-probe.sql <<'PROBE_SQL'
          do $probe$
          declare
            v_org uuid;
          begin
            select id into v_org from public.organizations limit 1;
            if v_org is null then
              raise notice 'no organization to write a probe row against';
              return;
            end if;
            begin
              insert into public.activity_events
                (organization_id, event_type, entity_type, description)
              values (v_org, 'ai_account.changed', 'ai_account', 'probe row, deleted immediately');
              delete from public.activity_events where description = 'probe row, deleted immediately';
              raise notice 'activity_events insert as %: OK, probe row deleted', current_user;
            exception when others then
              raise notice 'activity_events insert as %: % %', current_user, sqlstate, sqlerrm;
            end;
          end
          $probe$;
          PROBE_SQL
          psql "$DB_URL" -q -f /tmp/audit-write-probe.sql
          echo ""
          echo "And the call itself, as the owner would make it, inside a transaction that is"
          echo "rolled back whatever happens. This reports the SQLSTATE and message the console"
          echo "receives, which is the one fact none of the queries above can produce."
          cat > /tmp/remove-account-probe.sql <<'REMOVE_SQL'
          begin;
          do $probe$
          declare
            v_org uuid;
            v_owner uuid;
            v_account uuid;
            v_result boolean;
            v_uid uuid;
            v_can boolean;
            v_member record;
          begin
            /*
             * The first attempt picked the earliest owner membership and found
             * an organization with no accounts at all — the definitive call
             * never ran. Start from the accounts instead: the organization that
             * holds one, joined to an owner OF THAT organization, is the exact
             * shape the console's failing request has.
             */
            select a.organization_id, m.user_id, a.id
              into v_org, v_owner, v_account
              from public.ai_accounts a
              join public.organization_members m
                on m.organization_id = a.organization_id and m.role = 'owner'
             order by a.created_at
             limit 1;
            if v_account is null then
              raise notice 'no AI account with an owner membership anywhere — nothing to test';
              return;
            end if;
            raise notice 'testing org % account % as user %', v_org, v_account, v_owner;

            for v_member in
              select m.user_id, m.role from public.organization_members m
               where m.organization_id = v_org order by m.created_at
            loop
              raise notice '  member % role %', v_member.user_id, v_member.role;
            end loop;

            perform set_config('request.jwt.claim.sub', v_owner::text, true);
            perform set_config('request.jwt.claims',
              json_build_object('sub', v_owner::text, 'role', 'authenticated')::text, true);
            set local role authenticated;

            /* Whether the identity plumbing itself resolves under this role is
               half the diagnosis: a NULL here means the probe's JWT emulation
               (not production) is what any refusal below is measuring. */
            begin
              select auth.uid() into v_uid;
              raise notice 'auth.uid() under authenticated: %', coalesce(v_uid::text, 'NULL');
            exception when others then
              raise notice 'auth.uid() raised: % %', sqlstate, sqlerrm;
            end;
            begin
              select public.can_manage_organization(v_org) into v_can;
              raise notice 'can_manage_organization: %', v_can;
            exception when others then
              raise notice 'can_manage_organization raised: % %', sqlstate, sqlerrm;
            end;

            begin
              select public.remove_ai_account(v_org, v_account) into v_result;
              raise notice 'remove_ai_account as owner: returned % (rolled back)', v_result;
            exception when others then
              raise notice 'remove_ai_account as owner: % %', sqlstate, sqlerrm;
            end;
            reset role;
          end
          $probe$;
          rollback;
          REMOVE_SQL
          psql "$DB_URL" -q -f /tmp/remove-account-probe.sql
          echo ""
          echo "Bot creation, the same way: the console's Create Bot ends at register_bot"
          echo "through ensureProviderBot, which swallows every database error into a"
          echo "'skipped' the route answers 200 for. This calls register_bot as the owner"
          echo "and reports what actually happens, rolled back either way."
          cat > /tmp/register-bot-probe.sql <<'BOT_SQL'
          begin;
          do $probe$
          declare
            v_org uuid;
            v_owner uuid;
            v_row record;
            v_count integer;
          begin
            select a.organization_id, m.user_id
              into v_org, v_owner
              from public.ai_accounts a
              join public.organization_members m
                on m.organization_id = a.organization_id and m.role = 'owner'
             order by a.created_at
             limit 1;
            if v_org is null then
              raise notice 'no organization with accounts to test bot creation in';
              return;
            end if;

            select count(*) into v_count from public.bots b where b.organization_id = v_org;
            raise notice 'org % holds % bot rows', v_org, v_count;

            perform set_config('request.jwt.claim.sub', v_owner::text, true);
            perform set_config('request.jwt.claims',
              json_build_object('sub', v_owner::text, 'role', 'authenticated')::text, true);
            set local role authenticated;

            begin
              select * into v_row from public.register_bot(
                v_org, 'Probe Bot', 'anthropic'::public.bot_provider, 'claude-opus-5',
                'SOFTWAREFACTORY_CLAUDE_CODE_OAUTH_TOKEN', null,
                'Probe row, rolled back with this transaction.'
              );
              raise notice 'register_bot as owner: created % (rolled back)', v_row.id;
            exception when others then
              raise notice 'register_bot as owner: % %', sqlstate, sqlerrm;
            end;
            reset role;
          end
          $probe$;
          rollback;
          BOT_SQL
          psql "$DB_URL" -q -f /tmp/register-bot-probe.sql
          echo ""
          echo "The Agents page's chain: per organization, how many logical agents and"
          echo "enabled model configurations exist, and whether assigning one to an agent"
          echo "works as the owner — rolled back either way."
          cat > /tmp/agent-assignment-probe.sql <<'AGENT_SQL'
          begin;
          do $probe$
          declare
            v_org uuid;
            v_owner uuid;
            v_agent uuid;
            v_model text;
            v_row record;
            v_counts record;
          begin
            for v_counts in
              select o.id,
                     (select count(*) from public.agents a where a.organization_id = o.id) as agents,
                     (select count(*) from public.provider_model_configurations c
                       where c.organization_id = o.id and c.enabled) as enabled_models
                from public.organizations o
               order by o.created_at
            loop
              raise notice 'org % holds % agents, % enabled model configurations',
                v_counts.id, v_counts.agents, v_counts.enabled_models;
            end loop;

            select a.organization_id, a.id, m.user_id
              into v_org, v_agent, v_owner
              from public.agents a
              join public.organization_members m
                on m.organization_id = a.organization_id and m.role = 'owner'
             order by a.created_at
             limit 1;
            if v_agent is null then
              raise notice 'no agent with an owner membership anywhere - nothing to test';
              return;
            end if;
            select c.model into v_model
              from public.provider_model_configurations c
             where c.organization_id = v_org
               and c.provider = 'anthropic'::public.connection_provider
               and c.enabled
             order by c.model
             limit 1;
            if v_model is null then
              raise notice 'org % has no enabled anthropic model to assign', v_org;
              return;
            end if;

            perform set_config('request.jwt.claim.sub', v_owner::text, true);
            perform set_config('request.jwt.claims',
              json_build_object('sub', v_owner::text, 'role', 'authenticated')::text, true);
            set local role authenticated;
            begin
              select * into v_row from public.set_agent_provider_assignment(v_agent, 'anthropic', v_model);
              raise notice 'set_agent_provider_assignment as owner: agent % now % / % (rolled back)',
                v_row.id, v_row.provider, v_row.model;
            exception when others then
              raise notice 'set_agent_provider_assignment as owner: % %', sqlstate, sqlerrm;
            end;
            reset role;
          end
          $probe$;
          rollback;
          AGENT_SQL
          psql "$DB_URL" -q -f /tmp/agent-assignment-probe.sql
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select 'column' as kind, table_name || '.' || column_name as object
              from information_schema.columns
             where table_schema = 'public'
               and (table_name, column_name) in (
                 ('projects','engineering_priority'), ('projects','strategic_focus'),
                 ('projects','engineering_paused'), ('projects','engineering_pause_reason'),
                 ('projects','maximum_concurrent_runs'),
                 ('organizations','maximum_concurrent_runs'),
                 ('agent_runs','review_status'), ('agent_runs','review_note'))
            union all
            select 'table', c.relname from pg_class c
              join pg_namespace n on n.oid = c.relnamespace
             where n.nspname = 'public' and c.relkind = 'r'
               and c.relname in ('scheduling_decisions','provider_capacity_limits')
            union all
            select 'function', p.proname from pg_proc p
              join pg_namespace n on n.oid = p.pronamespace
             where n.nspname = 'public'
               and p.proname in (
                 'archive_project','unarchive_project','update_project_details',
                 'set_project_engineering_priority','set_project_engineering_pause',
                 'focus_portfolio_engineering','set_portfolio_capacity_limits',
                 'update_agent_run_review','delete_agent_run')
            order by 1, 2;"
          echo ""
          echo "The Agentic SDLC lifecycle (20260821000100 and 20260821000200), asked about"
          echo "directly. These two files are newer than the measured ledger table in"
          echo "AI/HOSTED_APPLY_RUNBOOK.md, so they are deliberately kept out of the query"
          echo "above: that one reports a measurement, and appending an unmeasured version"
          echo "to it would turn evidence into assertion. present = f for every row is the"
          echo "expected reading before scope=lifecycle runs, and t for every row after."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            with lifecycle(kind, object, marker) as (values
              ('enum',     'sdlc_stage',                  'MONITORING'),
              ('enum',     'gate_kind',                   'HUMAN'),
              ('enum',     'gate_state',                  'APPROVED'),
              ('enum',     'activity_event_type',         'lifecycle.gate_approved'),
              ('table',    'graph_gates',                 null),
              ('column',   'graph_nodes.lifecycle_stage', null),
              ('column',   'graphs.max_iterations',       null),
              ('column',   'graph_edges.is_feedback',     null),
              ('column',   'node_runs.confidence',        null),
              ('function', 'open_node_gate_as_worker',    null),
              ('function', 'decide_node_gate',            null),
              ('function', 'advance_graph_iteration',     null),
              ('body',     'claim_planned_graph',         'graph_gates'),
              ('body',     'list_graph_runs',             'gate_anchor_count')
            )
            select lifecycle.kind,
                   lifecycle.object,
                   case lifecycle.kind
                     when 'enum' then exists (
                       select 1 from pg_type kind_type
                         join pg_namespace space on space.oid = kind_type.typnamespace
                         join pg_enum label on label.enumtypid = kind_type.oid
                        where space.nspname = 'public'
                          and kind_type.typname = lifecycle.object
                          and label.enumlabel = lifecycle.marker)
                     when 'table' then exists (
                       select 1 from pg_class relation
                         join pg_namespace space on space.oid = relation.relnamespace
                        where space.nspname = 'public'
                          and relation.relkind = 'r'
                          and relation.relname = lifecycle.object)
                     when 'column' then exists (
                       select 1 from information_schema.columns
                        where table_schema = 'public'
                          and table_name = split_part(lifecycle.object, '.', 1)
                          and column_name = split_part(lifecycle.object, '.', 2))
                     when 'function' then exists (
                       select 1 from pg_proc routine
                         join pg_namespace space on space.oid = routine.pronamespace
                        where space.nspname = 'public'
                          and routine.proname = lifecycle.object)
                     else exists (
                       select 1 from pg_proc routine
                         join pg_namespace space on space.oid = routine.pronamespace
                        where space.nspname = 'public'
                          and routine.proname = lifecycle.object
                          and strpos(pg_get_functiondef(routine.oid), lifecycle.marker) > 0)
                   end as present
              from lifecycle;"
          echo "The last two rows matter most: both files only REPLACE claim_planned_graph and"
          echo "list_graph_runs, so their existence proves nothing. Those rows ask whether the"
          echo "replacement body is the lifecycle-aware one."
          echo ""
          echo "And whether graph_gates is protected, if it exists at all. A new table without"
          echo "row level security is an exposed table, and the ledger cannot say either way."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select relation.relname,
                   relation.relrowsecurity as rls_enabled,
                   relation.relforcerowsecurity as rls_forced,
                   (select count(*) from pg_policy where polrelid = relation.oid) as policies,
                   has_table_privilege('anon', relation.oid, 'SELECT') as anon_may_select,
                   has_table_privilege('service_role', relation.oid, 'INSERT') as service_role_may_insert
              from pg_class relation
              join pg_namespace space on space.oid = relation.relnamespace
             where space.nspname = 'public'
               and relation.relname = 'graph_gates';"
          echo "No row at all means the table is absent and 20260821000200 has not run here —"
          echo "this asks pg_class, so a missing table is zero rows rather than an error."
          echo "Expected once applied: rls_enabled t, rls_forced t, one SELECT policy,"
          echo "anon_may_select f, and service_role_may_insert f — the worker writes only"
          echo "through open_node_gate_as_worker, never through a table grant."

      - name: Apply the project control migrations surgically (scope=project-controls)
        # The 20260815 range plus the two run-review files: everything the
        # project detail page's priority, focus, stop and cancel controls need,
        # and the Runs page's review and delete.
        #
        # Exactly the three files run 32068262957's probe showed missing, and
        # no others.
        #
        # The probe is why this list is short. The ledger says the whole
        # 20260815 range is outstanding; the database says otherwise — every
        # project scheduling column, and set_project_engineering_pause,
        # set_project_engineering_priority, focus_portfolio_engineering,
        # set_portfolio_capacity_limits and update_project_details, are already
        # live. Applying that range re-runs DDL for objects that exist, which
        # is exactly how run 32068091179 failed.
        #
        # Missing, and therefore here: archive_project and unarchive_project —
        # the project detail page's cancel and restore — the run review and
        # deletion pair, and run archiving with the report lifecycle. Every file
        # is idempotent: enum additions are `add value if not exists`, functions
        # are `create or replace`, and the two column-adding files use
        # `add column if not exists` with their constraints dropped before they
        # are added.
        #
        # 20260817001000 also drops and recreates list_agent_runs to add
        # p_include_archived. The parameter has a default, so the existing
        # two-argument call sites keep working through the swap.
        if: ${{ inputs.scope == 'project-controls' }}
        run: |
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          repair() {
            if [ "$MODE" = "token" ]; then
              npx supabase migration repair --status applied "$1"
            else
              npx supabase migration repair --status applied "$1" --db-url "$DB_URL"
            fi
          }
          # Each file is applied and immediately recorded, so an interruption
          # leaves a ledger that is behind rather than one that lies.
          for FILE in \
            supabase/migrations/20260815000700_project_archive_operation.sql \
            supabase/migrations/20260817000200_run_review_activity_types.sql \
            supabase/migrations/20260817000300_run_review_and_deletion.sql \
            supabase/migrations/20260817000900_run_and_report_lifecycle_types.sql \
            supabase/migrations/20260817001000_archive_runs_and_clear_reports.sql
          do
            VERSION=$(basename "$FILE" | cut -d_ -f1)
            echo "Applying ${FILE}"
            psql "$DB_URL" -v ON_ERROR_STOP=1 -q -f "$FILE"
            repair "$VERSION"
          done

      - name: Apply the pipeline selection migration (scope=pipeline-selection)
        # One file, because one feature needs it. Running broker-functions to
        # reach it would re-run unrelated migrations, each idempotent but each
        # still a change to production nobody asked for in this run.
        #
        # 20260821000300 creates public.project_pipelines with RLS and FORCE
        # RLS, revokes every table privilege from anon, authenticated and
        # service_role, and adds select_project_pipeline,
        # deselect_project_pipeline and list_project_pipelines. It adds; it
        # alters nothing that exists. Every statement is idempotent —
        # `create table if not exists`, `create index if not exists`,
        # `alter type ... add value if not exists`, `create or replace
        # function` — and psql runs each autocommitted, which is what lets the
        # two new enum labels be added and then named from a function body
        # defined below them.
        if: ${{ inputs.scope == 'pipeline-selection' }}
        run: |
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          FILE=supabase/migrations/20260821000300_project_pipeline_selection.sql
          echo "Applying ${FILE}"
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -f "$FILE"
          if [ "$MODE" = "token" ]; then
            npx supabase migration repair --status applied 20260821000300
          else
            npx supabase migration repair --status applied 20260821000300 --db-url "$DB_URL"
          fi
          # Insurance: Supabase's DDL event trigger reloads the PostgREST
          # schema cache on its own, but a browser that saw PGRST202 seconds
          # ago must not keep seeing it.
          psql "$DB_URL" -q -c "NOTIFY pgrst, 'reload schema';"
          echo "Pipeline selection applied and recorded; the API schema cache was reloaded."

      - name: Apply the agent selection surgically (scope=agent-selection)
        # Exactly one file — 20260822000100 — the project_agents mirror of the
        # pipeline-selection scope above, and replay-safe the same way: enum
        # labels are `add value if not exists`, the table is `create table if
        # not exists`, indexes are `if not exists`, and the three definer
        # functions are `create or replace`.
        if: ${{ inputs.scope == 'agent-selection' }}
        run: |
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          FILE=supabase/migrations/20260822000100_project_agent_selection.sql
          echo "Applying ${FILE}"
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -f "$FILE"
          if [ "$MODE" = "token" ]; then
            npx supabase migration repair --status applied 20260822000100
          else
            npx supabase migration repair --status applied 20260822000100 --db-url "$DB_URL"
          fi
          # Insurance: Supabase's DDL event trigger reloads the PostgREST
          # schema cache on its own, but a browser that saw PGRST202 seconds
          # ago must not keep seeing it.
          psql "$DB_URL" -q -c "NOTIFY pgrst, 'reload schema';"
          echo "Agent selection applied and recorded; the API schema cache was reloaded."

      - name: Apply the exact bot account binding (scope=bot-account-binding)
        # Exactly one protected file. Its frozen hash is checked before any
        # database write, the immediately preceding migration must already be
        # in the ledger, and this version must not be there. Never replace this
        # scope with `all`: the hosted ledger is intentionally non-contiguous.
        if: ${{ inputs.scope == 'bot-account-binding' }}
        run: |
          if [ -z "$SUPABASE_DB_PASSWORD" ]; then
            echo "::error::The protected bot-account-binding scope requires SUPABASE_DB_PASSWORD for live catalog verification."
            exit 1
          fi
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          FILE=supabase/migrations/20260822000200_register_bot_for_ai_account.sql
          EXPECTED_SHA256=39c8a4ae633e2e45dc71a754225ca54c9ef9dd27036f7b68dca6371e1c394981
          ACTUAL_SHA256=$(sha256sum "$FILE" | cut -d' ' -f1)
          if [ "$ACTUAL_SHA256" != "$EXPECTED_SHA256" ]; then
            echo "::error::Protected migration hash mismatch; refusing production DDL."
            exit 1
          fi

          PREDECESSOR=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000100';")
          TARGET=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000200';")
          if [ "$PREDECESSOR" != "1" ] || [ "$TARGET" != "0" ]; then
            echo "::error::Hosted migration history is not the exact expected predecessor/target state."
            exit 1
          fi

          LEGACY_CATALOG_READY=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc "
            set search_path = pg_catalog;
            with expected(signature, definition_md5) as (values
              ('public.register_bot(uuid,text,public.bot_provider,text,text,text,text)', '318700d6d8d26603d9f5572f11d16e2f'),
              ('public.assign_bot(uuid,uuid,uuid,uuid)', '2398b395e3545628939f4f5b6461011d'),
              ('public.assign_bots_to_project(uuid,uuid,jsonb)', '4bf7d8d08c5cf938fba2fea2376839eb'),
              ('public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)', 'ed7c5cbe31543823161348d14d9c69d7'),
              ('public.set_bot_assignment_execution(uuid,uuid,text,text)', 'cab77271ac904b4e13105f3dd3a06335'),
              ('public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)', '77852950f29eb560c300e0ec24649ed9'),
              ('public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)', '6cd8b39bcae374515bedc66d5da31413')
            ), function_state as (
              select expected.*,
                     routine.oid,
                     routine.proowner,
                     routine.prokind,
                     routine.provolatile,
                     routine.prosecdef,
                     routine.proconfig,
                     routine.proacl,
                     routine_schema.nspname,
                     routine_language.lanname,
                     md5(pg_get_functiondef(routine.oid)) as actual_definition_md5
                from expected
                left join pg_proc routine on routine.oid = to_regprocedure(expected.signature)
                left join pg_namespace routine_schema on routine_schema.oid = routine.pronamespace
                left join pg_language routine_language on routine_language.oid = routine.prolang
            )
            select to_regrole('authenticated') is not null
               and to_regrole('anon') is not null
               and to_regrole('service_role') is not null
               and (select count(oid) = 7
                      and bool_and(
                        nspname = 'public'
                        and lanname = 'plpgsql'
                        and prokind = 'f'
                        and provolatile = 'v'
                        and prosecdef
                        and proconfig = array['search_path=pg_catalog']::text[]
                        and pg_get_userbyid(proowner) = 'postgres'
                        and actual_definition_md5 = definition_md5
                        and proacl is not null
                        and (select count(*) from aclexplode(proacl)) = 2
                        and exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantor = proowner
                             and acl.grantee = proowner
                             and acl.privilege_type = 'EXECUTE'
                             and not acl.is_grantable
                        )
                        and exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantor = proowner
                             and acl.grantee = to_regrole('authenticated')::oid
                             and acl.privilege_type = 'EXECUTE'
                             and not acl.is_grantable
                        )
                        and not has_function_privilege('anon', signature, 'EXECUTE')
                        and not has_function_privilege('service_role', signature, 'EXECUTE')
                        and not exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantee = 0 and acl.privilege_type = 'EXECUTE'
                        )
                      )
                    from function_state)
               and (
                 select count(*) = 7
                   from pg_proc routine
                   join pg_namespace routine_schema on routine_schema.oid = routine.pronamespace
                  where routine_schema.nspname = 'public'
                    and routine.proname in (
                      'register_bot', 'assign_bot', 'assign_bots_to_project',
                      'record_bot_readiness', 'set_bot_assignment_execution',
                      'update_bot_assignment', 'update_bot_assignment_configuration'
                    )
               );")
          if [ "$LEGACY_CATALOG_READY" != "t" ]; then
            echo "::error::Legacy register_bot/mutator definition, owner, language, kind, volatility, SECURITY DEFINER, search_path, overload, or ACL drifted; refusing production DDL."
            exit 1
          fi

          LEGACY_BEFORE=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atq -F '|' -c "
            select md5(pg_get_functiondef(routine.oid)),
                   routine.prosecdef,
                   coalesce(array_to_string(routine.proconfig, ','), ''),
                   coalesce(array_to_string(routine.proacl, ','), '')
              from pg_proc routine
             where routine.oid = 'public.register_bot(uuid,text,public.bot_provider,text,text,text,text)'::regprocedure;")
          if [ -z "$LEGACY_BEFORE" ]; then
            echo "::error::Legacy register_bot identity is missing; refusing the forward migration."
            exit 1
          fi

          MUTATORS_BEFORE=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atq -F '|' -c "
            select routine.oid::regprocedure::text,
                   md5(pg_get_functiondef(routine.oid)),
                   routine.prosecdef,
                   coalesce(array_to_string(routine.proconfig, ','), ''),
                   coalesce(array_to_string(routine.proacl, ','), '')
              from pg_proc routine
             where routine.oid in (
               'public.assign_bot(uuid,uuid,uuid,uuid)'::regprocedure,
               'public.assign_bots_to_project(uuid,uuid,jsonb)'::regprocedure,
               'public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)'::regprocedure,
               'public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)'::regprocedure,
               'public.set_bot_assignment_execution(uuid,uuid,text,text)'::regprocedure,
               'public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)'::regprocedure
             )
             order by 1;")
          if [ "$(printf '%s\n' "$MUTATORS_BEFORE" | wc -l | tr -d ' ')" != "6" ]; then
            echo "::error::A legacy assignment mutator identity is missing; refusing the forward migration."
            exit 1
          fi

          LEGACY_ACLS_BEFORE=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc "
            select not exists (
              select 1
                from (values
                  ('public.assign_bot(uuid,uuid,uuid,uuid)'),
                  ('public.assign_bots_to_project(uuid,uuid,jsonb)'),
                  ('public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)'),
                  ('public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)'),
                  ('public.set_bot_assignment_execution(uuid,uuid,text,text)'),
                  ('public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)')
                ) legacy(signature)
               where not has_function_privilege('authenticated', legacy.signature, 'EXECUTE')
                  or has_function_privilege('anon', legacy.signature, 'EXECUTE')
                  or has_function_privilege('public', legacy.signature, 'EXECUTE')
                  or has_function_privilege('service_role', legacy.signature, 'EXECUTE')
            );")
          if [ "$LEGACY_ACLS_BEFORE" != "t" ]; then
            echo "::error::Legacy mutator ACLs are not the expected authenticated-only cutover contract; refusing to normalize drift."
            exit 1
          fi

          CATALOG_DRIFT_BEFORE=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atq -c "
            select coalesce(string_agg(drift.object_identity, E'\n' order by drift.object_identity), '')
              from (
                select 'function:' || expected.signature as object_identity
                  from (values
                    ('ai_account_bot_credential_ref', 'public.ai_account_bot_credential_ref(public.bot_provider,text)'),
                    ('enforce_bot_ai_account_binding', 'public.enforce_bot_ai_account_binding()'),
                    ('ensure_ai_account_bot', 'public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)'),
                    ('increment_bot_revision', 'public.increment_bot_revision()'),
                    ('increment_bot_assignment_revision', 'public.increment_bot_assignment_revision()'),
                    ('assign_bots_to_project_checked', 'public.assign_bots_to_project_checked(uuid,uuid,jsonb)'),
                    ('update_bot_assignment_configuration_checked', 'public.update_bot_assignment_configuration_checked(uuid,uuid,uuid,bigint,jsonb,uuid,public.bot_assignment_status)'),
                    ('update_bot_assignment_checked', 'public.update_bot_assignment_checked(uuid,uuid,uuid,bigint,public.bot_assignment_status)'),
                    ('set_bot_assignment_execution_checked', 'public.set_bot_assignment_execution_checked(uuid,uuid,uuid,bigint,text,text)'),
                    ('record_bot_readiness_preserving_disabled', 'public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)')
                  ) expected(function_name, signature)
                  join pg_proc routine
                    on routine.proname = expected.function_name
                  join pg_namespace routine_schema
                    on routine_schema.oid = routine.pronamespace
                   and routine_schema.nspname = 'public'
                union all
                select 'trigger:' || trigger_schema.nspname || '.' || trigger_relation.relname || '.' || trigger_row.tgname
                  from (values
                    ('public.bots'::regclass, 'bots_ai_account_binding_coherent'),
                    ('public.bots'::regclass, 'bots_increment_revision'),
                    ('public.bot_assignments'::regclass, 'bot_assignments_increment_revision')
                  ) expected(relation_id, trigger_name)
                  join pg_trigger trigger_row
                    on trigger_row.tgname = expected.trigger_name
                   and not trigger_row.tgisinternal
                  join pg_class trigger_relation
                    on trigger_relation.oid = trigger_row.tgrelid
                  join pg_namespace trigger_schema
                    on trigger_schema.oid = trigger_relation.relnamespace
                   and trigger_schema.nspname = 'public'
                union all
                select 'column:' || expected.column_name
                  from (values
                    ('public.bots'::regclass, 'bots.revision'),
                    ('public.bot_assignments'::regclass, 'bot_assignments.revision')
                  ) expected(relation_id, column_name)
                  join pg_attribute column_row
                    on column_row.attrelid = expected.relation_id
                   and column_row.attname = split_part(expected.column_name, '.', 2)
                   and not column_row.attisdropped
                union all
                select 'constraint:' || expected.constraint_name
                  from (values
                    ('public.bots'::regclass, 'bots_revision_positive'),
                    ('public.bot_assignments'::regclass, 'bot_assignments_revision_positive')
                  ) expected(relation_id, constraint_name)
                  join pg_constraint constraint_row
                    on constraint_row.conrelid = expected.relation_id
                   and constraint_row.conname = expected.constraint_name
              ) drift;")
          if [ -n "$CATALOG_DRIFT_BEFORE" ]; then
            echo "::error::Target ledger is absent but bot-account-binding objects already exist; refusing CREATE OR REPLACE/DROP TRIGGER over catalog drift."
            printf '%s\n' "$CATALOG_DRIFT_BEFORE"
            exit 1
          fi

          echo "Applying ${FILE} at frozen SHA-256 ${EXPECTED_SHA256}"
          psql "$DB_URL" -v ON_ERROR_STOP=1 --single-transaction -q \
            -f "$FILE" \
            -c "insert into supabase_migrations.schema_migrations (version) values ('20260822000200');"

          LEGACY_AFTER=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atq -F '|' -c "
            select md5(pg_get_functiondef(routine.oid)),
                   routine.prosecdef,
                   coalesce(array_to_string(routine.proconfig, ','), ''),
                   coalesce(array_to_string(routine.proacl, ','), '')
              from pg_proc routine
             where routine.oid = 'public.register_bot(uuid,text,public.bot_provider,text,text,text,text)'::regprocedure;")
          if [ "$LEGACY_AFTER" != "$LEGACY_BEFORE" ]; then
            echo "::error::Legacy register_bot definition/security/search_path/ACL changed; stop and contain forward."
            exit 1
          fi

          MUTATORS_AFTER=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atq -F '|' -c "
            select routine.oid::regprocedure::text,
                   md5(pg_get_functiondef(routine.oid)),
                   routine.prosecdef,
                   coalesce(array_to_string(routine.proconfig, ','), ''),
                   coalesce(array_to_string(routine.proacl, ','), '')
              from pg_proc routine
             where routine.oid in (
               'public.assign_bot(uuid,uuid,uuid,uuid)'::regprocedure,
               'public.assign_bots_to_project(uuid,uuid,jsonb)'::regprocedure,
               'public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)'::regprocedure,
               'public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)'::regprocedure,
               'public.set_bot_assignment_execution(uuid,uuid,text,text)'::regprocedure,
               'public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)'::regprocedure
             )
             order by 1;")
          if [ "$MUTATORS_AFTER" != "$MUTATORS_BEFORE" ]; then
            echo "::error::A legacy mutator definition/security/search_path/ACL changed; stop and contain forward."
            exit 1
          fi

          EXPAND_FUNCTIONS_EXACT=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc "
            set search_path = pg_catalog;
            with expected(signature, definition_md5, volatility, execute_role) as (values
              ('public.ai_account_bot_credential_ref(public.bot_provider,text)', 'd652a3406462bd5e9fcc4ae6afabdc49', 'i', 'none'),
              ('public.assign_bots_to_project_checked(uuid,uuid,jsonb)', '8d881acabd1e1f28ea74c3efc22354f3', 'v', 'authenticated'),
              ('public.enforce_bot_ai_account_binding()', '1acb03fac46c74be02b8e6ea746e0181', 'v', 'none'),
              ('public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)', 'eeb186b89ac3ba86f3c44bbc611d300e', 'v', 'authenticated'),
              ('public.increment_bot_assignment_revision()', 'a38347e81d404d1e6e33782cbabcd16a', 'v', 'none'),
              ('public.increment_bot_revision()', '25974d86264401f18dc34d4f5e10be53', 'v', 'none'),
              ('public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)', '416c81badfba6c09091b2f187188a81f', 'v', 'service_role'),
              ('public.set_bot_assignment_execution_checked(uuid,uuid,uuid,bigint,text,text)', '33e2ca65ab238042321775c4144fef79', 'v', 'authenticated'),
              ('public.update_bot_assignment_checked(uuid,uuid,uuid,bigint,public.bot_assignment_status)', '1364d470f5bea2fa389f3d48de320504', 'v', 'authenticated'),
              ('public.update_bot_assignment_configuration_checked(uuid,uuid,uuid,bigint,jsonb,uuid,public.bot_assignment_status)', '237ca9363d7934b0c5d63733e5631ca7', 'v', 'authenticated')
            ), function_state as (
              select expected.*,
                     routine.oid,
                     routine.proowner,
                     routine.prokind,
                     routine.provolatile,
                     routine.prosecdef,
                     routine.proconfig,
                     routine.proacl,
                     routine_schema.nspname,
                     routine_language.lanname,
                     md5(pg_get_functiondef(routine.oid)) as actual_definition_md5
                from expected
                left join pg_proc routine on routine.oid = to_regprocedure(expected.signature)
                left join pg_namespace routine_schema on routine_schema.oid = routine.pronamespace
                left join pg_language routine_language on routine_language.oid = routine.prolang
            )
            select to_regrole('authenticated') is not null
               and to_regrole('anon') is not null
               and to_regrole('service_role') is not null
               and (select count(oid) = 10
                      and bool_and(
                        nspname = 'public'
                        and lanname = 'plpgsql'
                        and prokind = 'f'
                        and provolatile::text = volatility
                        and prosecdef
                        and proconfig = array['search_path=pg_catalog']::text[]
                        and pg_get_userbyid(proowner) = 'postgres'
                        and actual_definition_md5 = definition_md5
                        and proacl is not null
                        and (select count(*) from aclexplode(proacl)) =
                          case when execute_role = 'none' then 1 else 2 end
                        and exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantor = proowner
                             and acl.grantee = proowner
                             and acl.privilege_type = 'EXECUTE'
                             and not acl.is_grantable
                        )
                        and (
                          execute_role = 'none'
                          or exists (
                            select 1 from aclexplode(proacl) acl
                             where acl.grantor = proowner
                               and acl.grantee = to_regrole(execute_role)::oid
                               and acl.privilege_type = 'EXECUTE'
                               and not acl.is_grantable
                          )
                        )
                        and not exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantee <> proowner
                             and (execute_role = 'none' or acl.grantee <> to_regrole(execute_role)::oid)
                        )
                        and not has_function_privilege('anon', signature, 'EXECUTE')
                        and has_function_privilege('authenticated', signature, 'EXECUTE') =
                          (execute_role = 'authenticated')
                        and has_function_privilege('service_role', signature, 'EXECUTE') =
                          (execute_role = 'service_role')
                        and not exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantee = 0 and acl.privilege_type = 'EXECUTE'
                        )
                      )
                    from function_state)
               and (
                 select count(*) = 10
                   from pg_proc routine
                   join pg_namespace routine_schema on routine_schema.oid = routine.pronamespace
                  where routine_schema.nspname = 'public'
                    and routine.proname in (
                      'ai_account_bot_credential_ref', 'assign_bots_to_project_checked',
                      'enforce_bot_ai_account_binding', 'ensure_ai_account_bot',
                      'increment_bot_assignment_revision', 'increment_bot_revision',
                      'record_bot_readiness_preserving_disabled',
                      'set_bot_assignment_execution_checked', 'update_bot_assignment_checked',
                      'update_bot_assignment_configuration_checked'
                    )
               );")
          if [ "$EXPAND_FUNCTIONS_EXACT" != "t" ]; then
            echo "::error::New EXPAND function definition/owner/language/kind/volatility/SECURITY DEFINER/search_path/exact ACL catalog is not exact."
            exit 1
          fi

          VERIFIED=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc "
            select
              exists (
                select 1 from pg_proc routine
                 where routine.oid = 'public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)'::regprocedure
                   and routine.prosecdef
                   and routine.proconfig @> array['search_path=pg_catalog']::text[]
              )
              and has_function_privilege('authenticated', 'public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)', 'EXECUTE')
              and not has_function_privilege('anon', 'public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)', 'EXECUTE')
              and not has_function_privilege('public', 'public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)', 'EXECUTE')
              and not has_function_privilege('service_role', 'public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)', 'EXECUTE')
              and exists (
                select 1 from pg_proc routine
                 where routine.oid = 'public.ai_account_bot_credential_ref(public.bot_provider,text)'::regprocedure
                   and routine.prosecdef
                   and routine.proconfig @> array['search_path=pg_catalog']::text[]
              )
              and not has_function_privilege('public', 'public.ai_account_bot_credential_ref(public.bot_provider,text)', 'EXECUTE')
              and not has_function_privilege('anon', 'public.ai_account_bot_credential_ref(public.bot_provider,text)', 'EXECUTE')
              and not has_function_privilege('authenticated', 'public.ai_account_bot_credential_ref(public.bot_provider,text)', 'EXECUTE')
              and not has_function_privilege('service_role', 'public.ai_account_bot_credential_ref(public.bot_provider,text)', 'EXECUTE')
              and exists (
                select 1 from pg_proc routine
                 where routine.oid = 'public.enforce_bot_ai_account_binding()'::regprocedure
                   and routine.prosecdef
                   and routine.proconfig @> array['search_path=pg_catalog']::text[]
              )
              and not has_function_privilege('public', 'public.enforce_bot_ai_account_binding()', 'EXECUTE')
              and not has_function_privilege('anon', 'public.enforce_bot_ai_account_binding()', 'EXECUTE')
              and not has_function_privilege('authenticated', 'public.enforce_bot_ai_account_binding()', 'EXECUTE')
              and not has_function_privilege('service_role', 'public.enforce_bot_ai_account_binding()', 'EXECUTE')
              and exists (
                select 1 from pg_trigger trigger_row
                 where trigger_row.tgrelid = 'public.bots'::regclass
                   and trigger_row.tgname = 'bots_ai_account_binding_coherent'
                   and not trigger_row.tgisinternal
                   and trigger_row.tgenabled = 'O'
                   and trigger_row.tgtype = 23
                   and trigger_row.tgfoid = 'public.enforce_bot_ai_account_binding()'::regprocedure
                   and pg_get_triggerdef(trigger_row.oid) like
                     'CREATE TRIGGER bots_ai_account_binding_coherent BEFORE INSERT OR UPDATE OF organization_id, ai_account_id, provider, credential_ref ON public.bots FOR EACH ROW EXECUTE FUNCTION %enforce_bot_ai_account_binding()'
              )
              and exists (
                select 1
                  from pg_attribute column_row
                  join pg_attrdef default_row
                    on default_row.adrelid = column_row.attrelid
                   and default_row.adnum = column_row.attnum
                 where column_row.attrelid = 'public.bots'::regclass
                   and column_row.attname = 'revision'
                   and not column_row.attisdropped
                   and column_row.attnotnull
                   and column_row.atttypid = 'pg_catalog.int8'::regtype
                   and pg_get_expr(default_row.adbin, default_row.adrelid)
                     in ('1', '1::bigint', '''1''::bigint')
              )
              and exists (
                select 1 from pg_constraint constraint_row
                 where constraint_row.conrelid = 'public.bots'::regclass
                   and constraint_row.conname = 'bots_revision_positive'
                   and constraint_row.contype = 'c'
                   and pg_get_constraintdef(constraint_row.oid) like '%revision > 0%'
              )
              and exists (
                select 1 from pg_proc routine
                 where routine.oid = 'public.increment_bot_revision()'::regprocedure
                   and routine.prosecdef
                   and routine.proconfig @> array['search_path=pg_catalog']::text[]
              )
              and not has_function_privilege('public', 'public.increment_bot_revision()', 'EXECUTE')
              and not has_function_privilege('anon', 'public.increment_bot_revision()', 'EXECUTE')
              and not has_function_privilege('authenticated', 'public.increment_bot_revision()', 'EXECUTE')
              and not has_function_privilege('service_role', 'public.increment_bot_revision()', 'EXECUTE')
              and exists (
                select 1 from pg_trigger trigger_row
                 where trigger_row.tgrelid = 'public.bots'::regclass
                   and trigger_row.tgname = 'bots_increment_revision'
                   and not trigger_row.tgisinternal
                   and trigger_row.tgenabled = 'O'
                   and trigger_row.tgtype = 19
                   and trigger_row.tgfoid = 'public.increment_bot_revision()'::regprocedure
              )
              and exists (
                select 1
                  from pg_attribute column_row
                  join pg_attrdef default_row
                    on default_row.adrelid = column_row.attrelid
                   and default_row.adnum = column_row.attnum
                 where column_row.attrelid = 'public.bot_assignments'::regclass
                   and column_row.attname = 'revision'
                   and not column_row.attisdropped
                   and column_row.attnotnull
                   and column_row.atttypid = 'pg_catalog.int8'::regtype
                   and pg_get_expr(default_row.adbin, default_row.adrelid)
                     in ('1', '1::bigint', '''1''::bigint')
              )
              and exists (
                select 1 from pg_constraint constraint_row
                 where constraint_row.conrelid = 'public.bot_assignments'::regclass
                   and constraint_row.conname = 'bot_assignments_revision_positive'
                   and constraint_row.contype = 'c'
                   and pg_get_constraintdef(constraint_row.oid) like '%revision > 0%'
              )
              and exists (
                select 1 from pg_proc routine
                 where routine.oid = 'public.increment_bot_assignment_revision()'::regprocedure
                   and routine.prosecdef
                   and routine.proconfig @> array['search_path=pg_catalog']::text[]
              )
              and not has_function_privilege('public', 'public.increment_bot_assignment_revision()', 'EXECUTE')
              and not has_function_privilege('anon', 'public.increment_bot_assignment_revision()', 'EXECUTE')
              and not has_function_privilege('authenticated', 'public.increment_bot_assignment_revision()', 'EXECUTE')
              and not has_function_privilege('service_role', 'public.increment_bot_assignment_revision()', 'EXECUTE')
              and exists (
                select 1 from pg_trigger trigger_row
                 where trigger_row.tgrelid = 'public.bot_assignments'::regclass
                   and trigger_row.tgname = 'bot_assignments_increment_revision'
                   and not trigger_row.tgisinternal
                   and trigger_row.tgenabled = 'O'
                   and trigger_row.tgtype = 19
                   and trigger_row.tgfoid = 'public.increment_bot_assignment_revision()'::regprocedure
              )
              and (
                select count(*) = 5
                  from pg_proc routine
                 where routine.oid in (
                    'public.assign_bots_to_project_checked(uuid,uuid,jsonb)'::regprocedure,
                    'public.update_bot_assignment_configuration_checked(uuid,uuid,uuid,bigint,jsonb,uuid,public.bot_assignment_status)'::regprocedure,
                    'public.update_bot_assignment_checked(uuid,uuid,uuid,bigint,public.bot_assignment_status)'::regprocedure,
                    'public.set_bot_assignment_execution_checked(uuid,uuid,uuid,bigint,text,text)'::regprocedure,
                    'public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)'::regprocedure
                 )
                   and routine.prosecdef
                   and routine.proconfig @> array['search_path=pg_catalog']::text[]
              )
              and not exists (
                select 1
                  from (values
                    ('public.assign_bots_to_project_checked(uuid,uuid,jsonb)'),
                    ('public.update_bot_assignment_configuration_checked(uuid,uuid,uuid,bigint,jsonb,uuid,public.bot_assignment_status)'),
                    ('public.update_bot_assignment_checked(uuid,uuid,uuid,bigint,public.bot_assignment_status)'),
                    ('public.set_bot_assignment_execution_checked(uuid,uuid,uuid,bigint,text,text)')
                  ) expected(signature)
                 where not has_function_privilege('authenticated', expected.signature, 'EXECUTE')
                    or has_function_privilege('anon', expected.signature, 'EXECUTE')
                    or has_function_privilege('public', expected.signature, 'EXECUTE')
                     or has_function_privilege('service_role', expected.signature, 'EXECUTE')
              )
              and has_function_privilege(
                'service_role',
                'public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)',
                'EXECUTE'
              )
              and not has_function_privilege(
                'authenticated',
                'public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)',
                'EXECUTE'
              )
              and not has_function_privilege(
                'anon',
                'public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)',
                'EXECUTE'
              )
              and not has_function_privilege(
                'public',
                'public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)',
                'EXECUTE'
              )
              and not exists (
                select 1
                  from (values
                    ('public.assign_bot(uuid,uuid,uuid,uuid)'),
                    ('public.assign_bots_to_project(uuid,uuid,jsonb)'),
                    ('public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)'),
                    ('public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)'),
                    ('public.set_bot_assignment_execution(uuid,uuid,text,text)'),
                    ('public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)')
                  ) legacy(signature)
                 where not has_function_privilege('authenticated', legacy.signature, 'EXECUTE')
                    or has_function_privilege('anon', legacy.signature, 'EXECUTE')
                    or has_function_privilege('public', legacy.signature, 'EXECUTE')
                    or has_function_privilege('service_role', legacy.signature, 'EXECUTE')
              );")
          if [ "$VERIFIED" != "t" ]; then
            echo "::error::New RPC/column/trigger signature, ACL, SECURITY DEFINER, or search_path verification failed."
            exit 1
          fi

          RECORDED=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000200';")
          if [ "$RECORDED" != "1" ]; then
            echo "::error::The exact migration did not land once in the hosted ledger."
            exit 1
          fi
          psql "$DB_URL" -q -c "NOTIFY pgrst, 'reload schema';"
          echo "Exact bot account binding applied, verified, recorded once, and the API schema cache was reloaded."
          echo "::notice::This scope proves the ledger and catalog/ACL contract only."
          echo "::notice::Runtime create/bind/assign/configure/readiness/audit behavior, linked database lint, application health, and kill-switch/autonomy/worker containment remain mandatory post-apply release gates."

      - name: Apply the exact bot mutator CONTRACT (scope=bot-account-binding-contract)
        # CONTRACT is deliberately separate from EXPAND. It is safe only after
        # the exact checked-RPC application SHA is the latest successful
        # Vercel-bot Production deployment. The pre-connection gate proves that
        # GitHub deployment identity; the operator separately verifies the exact
        # Vercel project and signed-in owner journey before attesting.
        if: ${{ inputs.scope == 'bot-account-binding-contract' }}
        env:
          CONTRACT_APP_SHA: ${{ inputs.contract_app_sha }}
          CONTRACT_ACCEPTANCE: ${{ inputs.contract_acceptance }}
        run: |
          CHECKED_OUT_SHA=$(git rev-parse HEAD)
          if [[ ! "$CONTRACT_APP_SHA" =~ ^[a-f0-9]{40}$ ]] || [ "$CONTRACT_APP_SHA" != "$CHECKED_OUT_SHA" ]; then
            echo "::error::CONTRACT requires the exact checked-out 40-character application SHA already accepted in Vercel production."
            exit 1
          fi
          if [ "$CONTRACT_ACCEPTANCE" != "exact-app-vercel-accepted" ]; then
            echo "::error::CONTRACT requires the exact-app-vercel-accepted attestation after signed-in production acceptance."
            exit 1
          fi
          echo "::notice::Operator attests that exact application ${CONTRACT_APP_SHA} is the healthy Vercel production deployment and passed signed-in owner acceptance."

          if [ -z "$SUPABASE_DB_PASSWORD" ]; then
            echo "::error::The protected bot-account-binding-contract scope requires SUPABASE_DB_PASSWORD for live catalog verification."
            exit 1
          fi
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi

          FILE=supabase/migrations/20260822000300_contract_bot_mutator_acls.sql
          EXPECTED_SHA256=e3bad45af18ed07d3ab7adcfc9a326103fc09fd2b398f664c733de73fac7c1e2
          ACTUAL_SHA256=$(sha256sum "$FILE" | cut -d' ' -f1)
          if [ "$ACTUAL_SHA256" != "$EXPECTED_SHA256" ]; then
            echo "::error::Protected CONTRACT migration hash mismatch; refusing production DDL."
            exit 1
          fi

          PREDECESSOR=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000200';")
          TARGET=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000300';")
          if [ "$PREDECESSOR" != "1" ] || [ "$TARGET" != "0" ]; then
            echo "::error::Hosted migration history is not the exact EXPAND-once/CONTRACT-absent state."
            exit 1
          fi

          CATALOG_READY=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc "
            set search_path = pg_catalog;
            with expected_functions(signature, definition_md5, execute_role) as (values
              ('public.ai_account_bot_credential_ref(public.bot_provider,text)', 'd652a3406462bd5e9fcc4ae6afabdc49', 'none'),
              ('public.assign_bot(uuid,uuid,uuid,uuid)', '2398b395e3545628939f4f5b6461011d', 'authenticated'),
              ('public.assign_bots_to_project(uuid,uuid,jsonb)', '4bf7d8d08c5cf938fba2fea2376839eb', 'authenticated'),
              ('public.assign_bots_to_project_checked(uuid,uuid,jsonb)', '8d881acabd1e1f28ea74c3efc22354f3', 'authenticated'),
              ('public.enforce_bot_ai_account_binding()', '1acb03fac46c74be02b8e6ea746e0181', 'none'),
              ('public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)', 'eeb186b89ac3ba86f3c44bbc611d300e', 'authenticated'),
              ('public.increment_bot_assignment_revision()', 'a38347e81d404d1e6e33782cbabcd16a', 'none'),
              ('public.increment_bot_revision()', '25974d86264401f18dc34d4f5e10be53', 'none'),
              ('public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)', 'ed7c5cbe31543823161348d14d9c69d7', 'authenticated'),
              ('public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)', '416c81badfba6c09091b2f187188a81f', 'service_role'),
              ('public.set_bot_assignment_execution(uuid,uuid,text,text)', 'cab77271ac904b4e13105f3dd3a06335', 'authenticated'),
              ('public.set_bot_assignment_execution_checked(uuid,uuid,uuid,bigint,text,text)', '33e2ca65ab238042321775c4144fef79', 'authenticated'),
              ('public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)', '77852950f29eb560c300e0ec24649ed9', 'authenticated'),
              ('public.update_bot_assignment_checked(uuid,uuid,uuid,bigint,public.bot_assignment_status)', '1364d470f5bea2fa389f3d48de320504', 'authenticated'),
              ('public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)', '6cd8b39bcae374515bedc66d5da31413', 'authenticated'),
              ('public.update_bot_assignment_configuration_checked(uuid,uuid,uuid,bigint,jsonb,uuid,public.bot_assignment_status)', '237ca9363d7934b0c5d63733e5631ca7', 'authenticated')
            ), function_state as (
              select expected.*,
                     routine.oid,
                     routine.proowner,
                     routine.prosecdef,
                     routine.proconfig,
                     routine.proacl,
                     md5(pg_get_functiondef(routine.oid)) as actual_definition_md5
                from expected_functions expected
                left join pg_proc routine
                  on routine.oid = to_regprocedure(expected.signature)
            )
            select
              (select count(oid) = 16
                 and bool_and(
                   actual_definition_md5 = definition_md5
                   and prosecdef
                   and proconfig = array['search_path=pg_catalog']::text[]
                   and pg_get_userbyid(proowner) = 'postgres'
                   and proacl is not null
                   and (select count(*) from aclexplode(proacl)) =
                     case when execute_role = 'none' then 1 else 2 end
                   and exists (
                     select 1 from aclexplode(proacl) acl
                      where acl.grantor = proowner
                        and acl.grantee = proowner
                        and acl.privilege_type = 'EXECUTE'
                        and not acl.is_grantable
                   )
                   and (
                     execute_role = 'none'
                     or exists (
                       select 1 from aclexplode(proacl) acl
                        where acl.grantor = proowner
                          and acl.grantee = to_regrole(execute_role)::oid
                          and acl.privilege_type = 'EXECUTE'
                          and not acl.is_grantable
                     )
                   )
                   and not exists (
                     select 1 from aclexplode(proacl) acl
                      where acl.grantee <> proowner
                        and (execute_role = 'none' or acl.grantee <> to_regrole(execute_role)::oid)
                   )
                   and not has_function_privilege('anon', signature, 'EXECUTE')
                   and has_function_privilege('authenticated', signature, 'EXECUTE') =
                     (execute_role = 'authenticated')
                   and has_function_privilege('service_role', signature, 'EXECUTE') =
                     (execute_role = 'service_role')
                   and not exists (
                     select 1 from aclexplode(proacl) acl
                      where acl.grantee = 0 and acl.privilege_type = 'EXECUTE'
                   )
                 )
               from function_state)
              and (
                select count(*) = 16
                  from pg_proc routine
                  join pg_namespace routine_schema on routine_schema.oid = routine.pronamespace
                 where routine_schema.nspname = 'public'
                   and routine.proname in (
                     'ai_account_bot_credential_ref', 'assign_bot',
                     'assign_bots_to_project', 'assign_bots_to_project_checked',
                     'enforce_bot_ai_account_binding', 'ensure_ai_account_bot',
                     'increment_bot_assignment_revision', 'increment_bot_revision',
                     'record_bot_readiness', 'record_bot_readiness_preserving_disabled',
                     'set_bot_assignment_execution', 'set_bot_assignment_execution_checked',
                     'update_bot_assignment', 'update_bot_assignment_checked',
                     'update_bot_assignment_configuration',
                     'update_bot_assignment_configuration_checked'
                   )
              )
              and (
                select count(column_row.attnum) = 2
                   and count(default_row.oid) = 2
                   and count(constraint_row.oid) = 2
                   and bool_and(
                     column_row.atttypid = 'int8'::regtype
                     and column_row.attnotnull
                     and column_row.attidentity = ''
                     and column_row.attgenerated = ''
                     and coalesce(
                       pg_get_expr(default_row.adbin, default_row.adrelid)
                         in ('1', '1::bigint', '''1''::bigint'),
                       false
                     )
                     and constraint_row.contype = 'c'
                     and constraint_row.convalidated
                     and not constraint_row.connoinherit
                     and pg_get_constraintdef(constraint_row.oid) = 'CHECK ((revision > 0))'
                   )
                  from (values
                    ('public.bot_assignments'::regclass, 'revision', 'bot_assignments_revision_positive'),
                    ('public.bots'::regclass, 'revision', 'bots_revision_positive')
                  ) expected(relation_id, column_name, constraint_name)
                  left join pg_attribute column_row
                    on column_row.attrelid = expected.relation_id
                   and column_row.attname = expected.column_name
                   and not column_row.attisdropped
                  left join pg_attrdef default_row
                    on default_row.adrelid = column_row.attrelid
                   and default_row.adnum = column_row.attnum
                  left join pg_constraint constraint_row
                    on constraint_row.conrelid = expected.relation_id
                   and constraint_row.conname = expected.constraint_name
              )
              and (
                select count(trigger_row.oid) = 3
                   and bool_and(
                     trigger_row.tgenabled = 'O'
                     and trigger_row.tgtype = expected.trigger_type
                     and trigger_row.tgfoid = expected.function_id
                     and md5(pg_get_triggerdef(trigger_row.oid)) = expected.definition_md5
                   )
                  from (values
                    ('public.bot_assignments'::regclass, 'bot_assignments_increment_revision', 19::smallint,
                     'public.increment_bot_assignment_revision()'::regprocedure, 'e7c4e60faa83da3f09d578815e47016b'),
                    ('public.bots'::regclass, 'bots_ai_account_binding_coherent', 23::smallint,
                     'public.enforce_bot_ai_account_binding()'::regprocedure, 'ae174d48495938eb6aa93a1340ae4680'),
                    ('public.bots'::regclass, 'bots_increment_revision', 19::smallint,
                     'public.increment_bot_revision()'::regprocedure, '4bb567fb61d925c6a7b8dffaf9218c4f')
                  ) expected(relation_id, trigger_name, trigger_type, function_id, definition_md5)
                  left join pg_trigger trigger_row
                    on trigger_row.tgrelid = expected.relation_id
                   and trigger_row.tgname = expected.trigger_name
                   and not trigger_row.tgisinternal
              );")
          if [ "$CATALOG_READY" != "t" ]; then
            echo "::error::The live function/revision/constraint/trigger catalog is not the exact 20260822000200 EXPAND state."
            exit 1
          fi

          DEFINITIONS_BEFORE=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atq -F '|' -c "
            set search_path = pg_catalog;
            select routine.oid::regprocedure::text,
                   md5(pg_get_functiondef(routine.oid)),
                   routine.prosecdef,
                   coalesce(array_to_string(routine.proconfig, ','), ''),
                   pg_get_userbyid(routine.proowner)
              from pg_proc routine
             where routine.oid in (
               'public.assign_bot(uuid,uuid,uuid,uuid)'::regprocedure,
               'public.assign_bots_to_project(uuid,uuid,jsonb)'::regprocedure,
               'public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)'::regprocedure,
               'public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)'::regprocedure,
               'public.set_bot_assignment_execution(uuid,uuid,text,text)'::regprocedure,
               'public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)'::regprocedure
             )
             order by 1;")
          if [ "$(printf '%s\n' "$DEFINITIONS_BEFORE" | wc -l | tr -d ' ')" != "6" ]; then
            echo "::error::A legacy bot mutator identity is missing before CONTRACT."
            exit 1
          fi

          echo "Applying ${FILE} at frozen SHA-256 ${EXPECTED_SHA256} after exact-app acceptance"
          psql "$DB_URL" -v ON_ERROR_STOP=1 --single-transaction -q \
            -f "$FILE" \
            -c "insert into supabase_migrations.schema_migrations (version) values ('20260822000300');"

          DEFINITIONS_AFTER=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atq -F '|' -c "
            set search_path = pg_catalog;
            select routine.oid::regprocedure::text,
                   md5(pg_get_functiondef(routine.oid)),
                   routine.prosecdef,
                   coalesce(array_to_string(routine.proconfig, ','), ''),
                   pg_get_userbyid(routine.proowner)
              from pg_proc routine
             where routine.oid in (
               'public.assign_bot(uuid,uuid,uuid,uuid)'::regprocedure,
               'public.assign_bots_to_project(uuid,uuid,jsonb)'::regprocedure,
               'public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)'::regprocedure,
               'public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)'::regprocedure,
               'public.set_bot_assignment_execution(uuid,uuid,text,text)'::regprocedure,
               'public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)'::regprocedure
             )
             order by 1;")
          if [ "$DEFINITIONS_AFTER" != "$DEFINITIONS_BEFORE" ]; then
            echo "::error::A legacy mutator definition/signature/owner/SECURITY DEFINER/search_path changed; stop and contain forward."
            exit 1
          fi

          ACLS_CLOSED=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc "
            select not exists (
              select 1
                from (values
                  ('public.assign_bot(uuid,uuid,uuid,uuid)'),
                  ('public.assign_bots_to_project(uuid,uuid,jsonb)'),
                  ('public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)'),
                  ('public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)'),
                  ('public.set_bot_assignment_execution(uuid,uuid,text,text)'),
                  ('public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)')
                ) legacy(signature)
                join pg_proc routine on routine.oid = to_regprocedure(legacy.signature)
               where has_function_privilege('authenticated', legacy.signature, 'EXECUTE')
                  or has_function_privilege('anon', legacy.signature, 'EXECUTE')
                  or has_function_privilege('service_role', legacy.signature, 'EXECUTE')
                  or (select count(*) from aclexplode(routine.proacl)) <> 1
                  or exists (
                    select 1 from aclexplode(routine.proacl) acl
                     where acl.grantee = 0 and acl.privilege_type = 'EXECUTE'
                  )
            );")
          if [ "$ACLS_CLOSED" != "t" ]; then
            echo "::error::Legacy mutator authenticated/public/anon/service_role EXECUTE closure failed."
            exit 1
          fi

          RECORDED=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000300';")
          if [ "$RECORDED" != "1" ]; then
            echo "::error::The exact CONTRACT migration did not land once in the hosted ledger."
            exit 1
          fi
          psql "$DB_URL" -q -c "NOTIFY pgrst, 'reload schema';"
          echo "Exact bot mutator CONTRACT applied, verified, recorded once, and the API schema cache was reloaded."

      - name: Apply the Agentic SDLC lifecycle surgically (scope=lifecycle)
        # Exactly two files — 20260821000100 and 20260821000200 — and no others.
        # Every other outstanding migration stays outstanding and separately
        # decidable, which is the whole reason this scope exists rather than a
        # `scope=all` that would sweep unrelated versions along with it.
        #
        # Replay-safe by construction, checked statement by statement: the three
        # new enum types are created inside DO blocks that first ask pg_type; the
        # activity labels are `add value if not exists`; every new column is
        # `add column if not exists`; the three graphs constraints are guarded by
        # DO blocks, because PostgreSQL has no `add constraint if not exists`;
        # graph_gates is `create table if not exists` with its policy dropped
        # before it is created; and the rest is `create or replace function`.
        # list_graph_runs is the one exception and it is explicit — its return
        # type changes, so the file drops it first with `drop function if exists`.
        #
        # The two files run as separate psql invocations on purpose. PostgreSQL
        # refuses to use an enum value in the transaction that added it, so the
        # labels in 20260821000100 must be committed before the function bodies
        # in 20260821000200 can write them.
        #
        # Prerequisites, confirmed by probe run 32531787440 rather than assumed:
        # 20260821000200 rebuilds claim_planned_graph and list_graph_runs from
        # 20260819001000 and 20260819000800, and that whole 20260819 range is
        # recorded on both sides of the hosted ledger.
        if: ${{ inputs.scope == 'lifecycle' }}
        run: |
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          repair() {
            if [ "$MODE" = "token" ]; then
              npx supabase migration repair --status applied "$1"
            else
              npx supabase migration repair --status applied "$1" --db-url "$DB_URL"
            fi
          }
          # Applied and recorded one at a time, so an interruption leaves a
          # ledger that is behind rather than one that lies.
          for FILE in \
            supabase/migrations/20260821000100_agentic_sdlc_activity_types.sql \
            supabase/migrations/20260821000200_agentic_sdlc_lifecycle.sql
          do
            VERSION=$(basename "$FILE" | cut -d_ -f1)
            echo "Applying ${FILE}"
            psql "$DB_URL" -v ON_ERROR_STOP=1 -q -f "$FILE"
            repair "$VERSION"
          done
          # Supabase's DDL event trigger reloads the PostgREST schema cache on
          # its own; this is insurance, so a browser that saw PGRST202 seconds
          # ago cannot keep seeing it for decide_node_gate.
          psql "$DB_URL" -q -c "NOTIFY pgrst, 'reload schema';"
          echo ""
          echo "What the run actually produced, read back rather than assumed."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select relation.relname,
                   relation.relrowsecurity as rls_enabled,
                   relation.relforcerowsecurity as rls_forced,
                   (select count(*) from pg_policy where polrelid = relation.oid) as policies,
                   has_table_privilege('anon', relation.oid, 'SELECT') as anon_may_select,
                   has_table_privilege('service_role', relation.oid, 'INSERT') as service_role_may_insert
              from pg_class relation
              join pg_namespace space on space.oid = relation.relnamespace
             where space.nspname = 'public'
               and relation.relname = 'graph_gates';"
          echo "rls_enabled and rls_forced must both be t, anon_may_select must be f, and"
          echo "service_role_may_insert must be f — the worker writes only through"
          echo "open_node_gate_as_worker, never through a table grant."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select routine.proname,
                   routine.prosecdef as security_definer,
                   has_function_privilege('authenticated', routine.oid, 'EXECUTE') as authenticated_may_execute,
                   has_function_privilege('service_role', routine.oid, 'EXECUTE') as service_role_may_execute
              from pg_proc routine
              join pg_namespace space on space.oid = routine.pronamespace
             where space.nspname = 'public'
               and routine.proname in (
                 'open_node_gate_as_worker', 'decide_node_gate', 'advance_graph_iteration')
             order by routine.proname;"
          echo "decide_node_gate is the member-facing one: authenticated must execute it."
          echo "open_node_gate_as_worker is the worker's: authenticated must NOT."
          echo "Exactly one claim_planned_graph overload should exist, and it must be the"
          echo "lifecycle-aware body — a second overload would mean the live claim is a"
          echo "resurrected older signature that reports no gates at all."
          psql "$DB_URL" -v ON_ERROR_STOP=1 -q -c "
            select pg_get_function_identity_arguments(routine.oid) as signature,
                   strpos(pg_get_functiondef(routine.oid), 'graph_gates') > 0 as knows_about_gates
              from pg_proc routine
              join pg_namespace space on space.oid = routine.pronamespace
              where space.nspname = 'public'
                and routine.proname = 'claim_planned_graph';"

      - name: Push the outstanding migrations (scope=all)
        # The pipe answers the CLI's confirmation prompt when it asks one;
        # in a non-interactive shell it is simply unread.
        # Named rather than "everything except broker-functions". The negative
        # form meant every scope added later fell through into a full
        # production push: run 32068262957 asked only for a read-only probe and
        # reached this step anyway. `db push` refused to act — it stops rather
        # than insert migrations before the remote's last one — so nothing was
        # applied, but the guard was one CLI safety check away from mattering.
        if: ${{ inputs.scope == 'all' }}
        run: |
          # `db push` has no per-file exclusion. Refuse the broad path until the
          # protected EXPAND and CONTRACT migrations have each already passed
          # their dedicated hash, predecessor, catalog, ACL, app-acceptance,
          # and ledger checks above in separate runs.
          if [ -z "$SUPABASE_DB_PASSWORD" ]; then
            echo "::error::scope=all requires SUPABASE_DB_PASSWORD to prove protected migrations 20260822000200 and 20260822000300 are already recorded."
            exit 1
          fi
          if [ "$MODE" != "dburl" ]; then
            ENC=$(node -e 'console.log(encodeURIComponent(process.env.SUPABASE_DB_PASSWORD))')
            echo "::add-mask::$ENC"
            DB_URL="postgresql://postgres.${PROJECT_REF}:${ENC}@aws-0-us-west-2.pooler.supabase.com:5432/postgres"
            echo "::add-mask::$DB_URL"
          fi
          PROTECTED_EXPAND=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000200';")
          PROTECTED_CONTRACT=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc \
            "select count(*) from supabase_migrations.schema_migrations where version = '20260822000300';")
          if [ "$PROTECTED_EXPAND" != "1" ]; then
            echo "::error::Protected migration 20260822000200 is not recorded exactly once. Apply it only through scope=bot-account-binding; broad push is forbidden."
            exit 1
          fi
          if [ "$PROTECTED_CONTRACT" != "1" ]; then
            echo "::error::Protected migration 20260822000300 is not recorded exactly once. Apply it only through scope=bot-account-binding-contract after exact-app Vercel acceptance; broad push is forbidden."
            exit 1
          fi
          PROTECTED_CATALOG_READY=$(psql "$DB_URL" -v ON_ERROR_STOP=1 -Atqc "
            set search_path = pg_catalog;
            with expected_functions(signature, definition_md5, execute_role) as (values
              ('public.ai_account_bot_credential_ref(public.bot_provider,text)', 'd652a3406462bd5e9fcc4ae6afabdc49', 'none'),
              ('public.assign_bot(uuid,uuid,uuid,uuid)', '2398b395e3545628939f4f5b6461011d', 'none'),
              ('public.assign_bots_to_project(uuid,uuid,jsonb)', '4bf7d8d08c5cf938fba2fea2376839eb', 'none'),
              ('public.assign_bots_to_project_checked(uuid,uuid,jsonb)', '8d881acabd1e1f28ea74c3efc22354f3', 'authenticated'),
              ('public.enforce_bot_ai_account_binding()', '1acb03fac46c74be02b8e6ea746e0181', 'none'),
              ('public.ensure_ai_account_bot(uuid,uuid,public.bot_provider,text,text,boolean,text,text)', 'eeb186b89ac3ba86f3c44bbc611d300e', 'authenticated'),
              ('public.increment_bot_assignment_revision()', 'a38347e81d404d1e6e33782cbabcd16a', 'none'),
              ('public.increment_bot_revision()', '25974d86264401f18dc34d4f5e10be53', 'none'),
              ('public.record_bot_readiness(uuid,uuid,public.bot_readiness,text)', 'ed7c5cbe31543823161348d14d9c69d7', 'none'),
              ('public.record_bot_readiness_preserving_disabled(uuid,uuid,uuid,bigint,uuid,public.bot_provider,text,text,text,public.bot_readiness,text)', '416c81badfba6c09091b2f187188a81f', 'service_role'),
              ('public.set_bot_assignment_execution(uuid,uuid,text,text)', 'cab77271ac904b4e13105f3dd3a06335', 'none'),
              ('public.set_bot_assignment_execution_checked(uuid,uuid,uuid,bigint,text,text)', '33e2ca65ab238042321775c4144fef79', 'authenticated'),
              ('public.update_bot_assignment(uuid,uuid,public.bot_assignment_status)', '77852950f29eb560c300e0ec24649ed9', 'none'),
              ('public.update_bot_assignment_checked(uuid,uuid,uuid,bigint,public.bot_assignment_status)', '1364d470f5bea2fa389f3d48de320504', 'authenticated'),
              ('public.update_bot_assignment_configuration(uuid,uuid,jsonb,uuid,public.bot_assignment_status)', '6cd8b39bcae374515bedc66d5da31413', 'none'),
              ('public.update_bot_assignment_configuration_checked(uuid,uuid,uuid,bigint,jsonb,uuid,public.bot_assignment_status)', '237ca9363d7934b0c5d63733e5631ca7', 'authenticated')
            ), function_state as (
              select expected.*,
                     routine.oid,
                     routine.proowner,
                     routine.prokind,
                     routine.prosecdef,
                     routine.proconfig,
                     routine.proacl,
                     routine_schema.nspname,
                     routine_language.lanname,
                     md5(pg_get_functiondef(routine.oid)) as actual_definition_md5
                from expected_functions expected
                left join pg_proc routine on routine.oid = to_regprocedure(expected.signature)
                left join pg_namespace routine_schema on routine_schema.oid = routine.pronamespace
                left join pg_language routine_language on routine_language.oid = routine.prolang
            )
            select to_regrole('authenticated') is not null
               and to_regrole('anon') is not null
               and to_regrole('service_role') is not null
               and (select count(oid) = 16
                      and bool_and(
                        nspname = 'public'
                        and lanname = 'plpgsql'
                        and prokind = 'f'
                        and actual_definition_md5 = definition_md5
                        and prosecdef
                        and proconfig = array['search_path=pg_catalog']::text[]
                        and pg_get_userbyid(proowner) = 'postgres'
                        and proacl is not null
                        and (select count(*) from aclexplode(proacl)) =
                          case when execute_role = 'none' then 1 else 2 end
                        and exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantor = proowner
                             and acl.grantee = proowner
                             and acl.privilege_type = 'EXECUTE'
                             and not acl.is_grantable
                        )
                        and (
                          execute_role = 'none'
                          or exists (
                            select 1 from aclexplode(proacl) acl
                             where acl.grantor = proowner
                               and acl.grantee = to_regrole(execute_role)::oid
                               and acl.privilege_type = 'EXECUTE'
                               and not acl.is_grantable
                          )
                        )
                        and not exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantee <> proowner
                             and (execute_role = 'none' or acl.grantee <> to_regrole(execute_role)::oid)
                        )
                        and not has_function_privilege('anon', signature, 'EXECUTE')
                        and has_function_privilege('authenticated', signature, 'EXECUTE') =
                          (execute_role = 'authenticated')
                        and has_function_privilege('service_role', signature, 'EXECUTE') =
                          (execute_role = 'service_role')
                        and not exists (
                          select 1 from aclexplode(proacl) acl
                           where acl.grantee = 0 and acl.privilege_type = 'EXECUTE'
                        )
                      )
                    from function_state)
               and (
                 select count(*) = 16
                   from pg_proc routine
                   join pg_namespace routine_schema on routine_schema.oid = routine.pronamespace
                  where routine_schema.nspname = 'public'
                    and routine.proname in (
                      'ai_account_bot_credential_ref', 'assign_bot',
                      'assign_bots_to_project', 'assign_bots_to_project_checked',
                      'enforce_bot_ai_account_binding', 'ensure_ai_account_bot',
                      'increment_bot_assignment_revision', 'increment_bot_revision',
                      'record_bot_readiness', 'record_bot_readiness_preserving_disabled',
                      'set_bot_assignment_execution', 'set_bot_assignment_execution_checked',
                      'update_bot_assignment', 'update_bot_assignment_checked',
                      'update_bot_assignment_configuration',
                      'update_bot_assignment_configuration_checked'
                    )
               )
               and (
                 select count(column_row.attnum) = 2
                    and count(default_row.oid) = 2
                    and count(constraint_row.oid) = 2
                    and bool_and(
                      column_row.atttypid = 'int8'::regtype
                      and column_row.attnotnull
                      and column_row.attidentity = ''
                      and column_row.attgenerated = ''
                      and coalesce(
                        pg_get_expr(default_row.adbin, default_row.adrelid)
                          in ('1', '1::bigint', '''1''::bigint'),
                        false
                      )
                      and constraint_row.contype = 'c'
                      and constraint_row.convalidated
                      and not constraint_row.connoinherit
                      and pg_get_constraintdef(constraint_row.oid) = 'CHECK ((revision > 0))'
                    )
                   from (values
                     ('public.bot_assignments'::regclass, 'revision', 'bot_assignments_revision_positive'),
                     ('public.bots'::regclass, 'revision', 'bots_revision_positive')
                   ) expected(relation_id, column_name, constraint_name)
                   left join pg_attribute column_row
                     on column_row.attrelid = expected.relation_id
                    and column_row.attname = expected.column_name
                    and not column_row.attisdropped
                   left join pg_attrdef default_row
                     on default_row.adrelid = column_row.attrelid
                    and default_row.adnum = column_row.attnum
                   left join pg_constraint constraint_row
                     on constraint_row.conrelid = expected.relation_id
                    and constraint_row.conname = expected.constraint_name
               )
               and (
                 select count(trigger_row.oid) = 3
                    and bool_and(
                      trigger_row.tgenabled = 'O'
                      and trigger_row.tgtype = expected.trigger_type
                      and trigger_row.tgfoid = expected.function_id
                      and md5(pg_get_triggerdef(trigger_row.oid)) = expected.definition_md5
                    )
                   from (values
                     ('public.bot_assignments'::regclass, 'bot_assignments_increment_revision', 19::smallint,
                      'public.increment_bot_assignment_revision()'::regprocedure, 'e7c4e60faa83da3f09d578815e47016b'),
                     ('public.bots'::regclass, 'bots_ai_account_binding_coherent', 23::smallint,
                      'public.enforce_bot_ai_account_binding()'::regprocedure, 'ae174d48495938eb6aa93a1340ae4680'),
                     ('public.bots'::regclass, 'bots_increment_revision', 19::smallint,
                      'public.increment_bot_revision()'::regprocedure, '4bb567fb61d925c6a7b8dffaf9218c4f')
                   ) expected(relation_id, trigger_name, trigger_type, function_id, definition_md5)
                   left join pg_trigger trigger_row
                     on trigger_row.tgrelid = expected.relation_id
                    and trigger_row.tgname = expected.trigger_name
                    and not trigger_row.tgisinternal
               );")
          if [ "$PROTECTED_CATALOG_READY" != "t" ]; then
            echo "::error::Protected ledger rows exist but the live contracted function/ACL/revision/constraint/trigger catalog is not exact; broad push is forbidden."
            exit 1
          fi
          if [ "$MODE" = "token" ]; then
            echo "Y" | npx supabase db push --linked -p "$SUPABASE_DB_PASSWORD"
          else
            echo "Y" | npx supabase db push --db-url "$DB_URL"
          fi

      - name: List the remote ledger after — the run's own evidence
        run: |
          if [ "$MODE" = "token" ]; then
            npx supabase migration list --linked
          else
            npx supabase migration list --db-url "$DB_URL"
          fi
